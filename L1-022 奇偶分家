#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>

int main()
{
	int a = 0, b = 0;
	int n;
	int i, j;
	scanf("%d", &n);
	for (i = 0; i < n; i++)
	{
		scanf("%d", &j);
		if (j % 2 == 0)
		{
			a++;
		}
		else
		{
			b++;
		}
	}
	printf("%d %d ", b, a);
	return 0;
}  
