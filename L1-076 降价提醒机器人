#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
#include <stdlib.h>
#include <string>

int main()
{
	double m, n;
	int i, j;
	scanf("%d %lf\n", &j, &n);
	for (i = 0; i < j; i++)
	{
		scanf("%lf\n", &m);
		if (m > n)
			printf("On Sale! %.1lf\n", m);
	}
	return 0;
}
